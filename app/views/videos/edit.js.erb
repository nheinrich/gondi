if ($("div.video_form")[0]) {
  video.form.hide()
}

$("<%= escape_javascript(@selector) %>").before("<%= escape_javascript(render(:partial => 'form', :locals => { :video => @video })) %>").fadeIn('fast')
video.form.init()