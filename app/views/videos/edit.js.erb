if ($("div.video_form")[0]) {
  video.form.hide()
}

$("div.admin").hide().prepend("<%= escape_javascript(render(:partial => 'form', :locals => { :video => @video })) %>").fadeIn('fast')
video.form.init()